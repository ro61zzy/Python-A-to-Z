# Pypi and Pip (huh?)
# instead of building sth from scratch one can search for packages build by others on pypi.org
# to install a package (excel package) to use in our programme  open terminal window
# for executing various commands just below next to run
# type      pip install openpyxl     then enter